# Quality Guard

A simple quality tracking system.

## Usage

1. Define a quality standard using `QualityGuard.define_quality_standard`.
2. Track a quality metric using `QualityGuard.track_quality_metric`.
3. Get quality trends using `QualityGuard.get_quality_trends`.
